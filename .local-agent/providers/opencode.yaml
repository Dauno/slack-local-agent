name: opencode
type: agent_cli
shim:
  command: self
  args: [shim, opencode]
profiles:
  smoke:
    model: opencode/big-pickle
    agent: build
    approval: reject
  flash-reasoning:
    model: deepseek/deepseek-v4-flash
    approval: auto
    variant: max
